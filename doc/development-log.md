# 开发日志

更新日期：2026-06-26

## 项目起点

项目从 `doc/requirement.md` 出发，目标是做一个个人使用的瘦身助手 Web 应用。第一版定位为自己使用，不做多人账号体系，不接自动健康数据源，只支持手动录入。

## 对话中的需求变更记录

1. 使用范围
   - 第一版只给自己用。
   - 先做 Web，默认跨平台访问。
   - 不做传统账号体系，使用访问设备识别。

2. 访问保护
   - 首次访问设备需要输入一个访问密码。
   - 已信任设备后不再重复输入密码。
   - 密码暂时保存在服务器本地数据库中，只保存哈希。
   - 后续补充了访问密码修改和受信设备移除能力。

3. 数据录入
   - 只做手动录入。
   - 健康记录每天只记录一次，多次提交覆盖当天记录。
   - 跑步记录可以同一天多条。
   - 历史页需要支持补充历史记录，选择日期后进入对应日期的录入页。

4. 目标与反馈
   - 需要设置健康目标和跑步目标。
   - 跑步目标包括每周次数、每周跑量。
   - 健康数据和运动数据同等重要。
   - 首页需要反馈距离目标还有多远、按当前趋势还需多久、给出克制的鼓励文案。
   - BMI 可以显示，身高放到配置界面维护。

5. 配置中心
   - 参数需要可配置。
   - 身高、提醒邮箱、趋势估算下限、提醒规则、SMTP、访问保护均放入配置中心。
   - 趋势估算参数需要系统下限，不能随意设得过低。

6. 提醒
   - 接受邮件提醒和站内提醒。
   - 后续补充 SMTP 配置、测试邮件、邮件提醒失败状态保留。

7. UI 与语言
   - 网站必须是中文。
   - 风格要简洁。
   - 首页不要把“今日状态”放在最显眼的大卡片位置，改为右上角紧凑状态面板。
   - 宽屏下不要留下大面积右侧空白，主内容应使用可用宽度。

8. 数据可视化
   - 首页不能全是文字，需要方便观察的曲线图。
   - 健康曲线和运动曲线不能密密麻麻堆满多张小图。
   - 每类曲线改为单张主图，通过下拉框切换指标。
   - 曲线需要纵轴。
   - 鼠标悬浮到某一天需要浮现该日数值。

9. 跑步配速
   - 平均配速不应由用户手动填写。
   - 配速应由跑步时长 / 公里数自动计算。
   - 录入页和编辑页中的配速改为只读计算项。

10. 开发辅助
    - 原 MCP 浏览器能力在当前会话不可用。
    - 增加本地 Playwright 截图脚本，用真实浏览器渲染桌面、宽屏、移动端截图，辅助后续 UI 调整。

## 当前开发进度

### 已完成

- Epic 1：应用骨架、本地数据库、访问密码、受信设备基础能力。
- Epic 2：健康记录、跑步记录、历史浏览筛选、编辑和删除。
- Epic 3：目标设置、首页摘要、趋势、BMI、目标进度、预计达成时间、鼓励文案。
- Epic 4：配置中心、个人资料、趋势阈值、提醒规则、站内提醒、SMTP、邮件提醒、访问保护管理。
- 后续对话增量：
  - 历史页补录入口。
  - 首页健康/运动曲线。
  - hydration mismatch 处理。
  - 首页状态面板位置调整。
  - 本地 Playwright 截图脚本。
  - 宽屏主内容铺满。
  - 曲线下拉切换、纵轴、悬浮提示。
  - 跑步配速只读自动计算。

### 当前质量状态

最近一次完整验证：

```text
npm test：84 个测试通过
npm run typecheck：通过
npm run lint：通过
npm run build：通过
npm run ui:screenshot：通过
```

### 当前关键能力

- 中文 Next.js Web 应用。
- 本地 SQLite + Drizzle ORM。
- 设备信任 + 本地访问密码保护。
- 健康数据手动录入与覆盖。
- 跑步数据手动录入，配速自动计算。
- 历史记录筛选、补录、编辑、删除。
- 健康目标和跑步目标。
- 首页摘要、目标进度、趋势估算、BMI、鼓励文案。
- 健康曲线和运动曲线交互式展示。
- 配置中心。
- 站内提醒和 SMTP 邮件提醒。
- 本地截图回归脚本。

### 已知注意事项

- `npm install` 后 `npm audit` 仍报告 6 个 moderate 级别问题，尚未执行 `npm audit fix --force`，避免引入破坏性依赖变更。
- `.ui-screenshots/`、`.next-dev-logs/`、`.agents/` 属于本地运行或工具产物，不纳入版本库。
- 当前是个人单用户版本，访问保护不是面向公网多用户的账号系统。

